Place your site background image here as `background.jpg`.

When you run the dev server, the file will be available at `/background.jpg`.

Example: copy your image to

```
C:\Users\Dilip\TRANSCAPES\public\background.jpg
```

Then reload the site — it will be used as the default background.
